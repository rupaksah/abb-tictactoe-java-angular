package com.tictactoe.backend.model;

/**
 * TWO_PLAYER: both X and O are controlled by human users.
 * VS_COMPUTER: human plays X, the backend's computer opponent plays O.
 */
public enum GameMode {
    TWO_PLAYER,
    VS_COMPUTER
}
